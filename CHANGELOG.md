# Журнал изменений

## 2026-09-13 — Полное обновление 0.2.2.7 / 0.2.2.6

- Добавлено: на GitHub — zip игры `game-0.2.2.7` и MSI лаунчера `launcher-0.2.2.6`.
- Изменено: кнопки скачивания на сайте ведут на `KolesnicaVoyny-0.2.2.6.msi`.

## 2026-09-13 — Команды выкладки

- Добавлено: фразы «Обнови игру», «Обнови лаунчер», «Полное обновление» собирают zip/MSI и публикуют GitHub Releases.

## 2026-09-11 — Живые на табло

- Добавлено: после пропуска на арену — вулкан, Ерш и Бегемотик в сетке. Первое место засчитывается, только если своих не убил. Дальше — правда в тоннеле. Скарабея нет.

## 2026-09-11 — Главное меню

- Изменено: титул на кадре каста, левая колонка пунктов и логотипа, марка Divan Games, версия в углу. Музыка меню — трек из `assets/music/cast`.

## 2026-09-11 — Звук двигателя

- Изменено: `001` набор, `002` луп хода, `003` сброс и воздух, `005` стоянка; `004` больше не играет.
- Исправлено: в воздухе сразу `003`, на посадке сразу `001` затем `002` без паузы и без обрывка лупа.
- Исправлено: луп хода и холостого без паузы на стыке; у паков обрезается тишина в начале и конце клипа.

## 2026-09-11 — Лента автопарка

- Изменено: чужие личные кузовы сюжета скрыты. Ближайшие закрытые остаются как цель, остальные спрятаны.

## 2026-09-11 — Выбор кузова и приглашение Янот

- Исправлено: после смены машины заезд больше не держит старый «Дьявол».
- Добавлено: пропуск на арену, Янот в сетке, «живые на табло».

## 2026-09-11 — Подарок Башкиру

- Добавлено: после ходовой — засада на перекрёстке. Первое место один раз отдаёт силовой модуль Башкиру. Дальше — приглашение на арену. Скарабея нет.

## 2026-09-11 — Ходовая Дьявола

- Добавлено: после корпуса — миссия на пыльном овале. Первое место один раз ставит ходовую и включает ручной режим. Чёрный ящик и Скарабей не возвращаются.

## 2026-09-10 — Миссия «Бронекузнец»

- Добавлено: в кампании первый заезд — охота на Бронекузнеца (каньон «Крушение»). Первое место один раз возвращает корпус Дьявола; чёрный ящик ещё у врага.

## 2026-09-10 — Кампания отдельно от свободного заезда

- Изменено: пролог после «любая кнопка» снова про арену. Сюжет Медведя стартует из меню «Кампания».

## 2026-09-10 — Вертикальный срез Медведя

- Добавлено: после комикса Медведя личный Camaro временно недоступен; в гараже цель «Найти Бронекузнеца». Свободный режим и старые сейвы не ломаются.

## 2026-09-10 — Пролог по диздоку

- Изменено: семь кадров `assets/data/cats/00` — герои, лица канона и личные машины. Текст — гараж, Лаурят, «Ночь зубов», семья, ограбление, временный кузов. Без Скарабея.

## 2026-09-10 — Кадры пролога Чёрного Пояса

- Добавлено: семь WebP 1920×1080 в `assets/data/cats/00` (`01`…`07`) для пролога после «любая кнопка».

## 2026-09-10 — Офлайн музыка и FX

- Изменено: десктоп не ходит на CDN за музыкой и эффектами; браузер по-прежнему может взять запасной URL.

## 2026-09-10 — Читы скрыты в публичном NSIS

- Изменено: в упакованном десктопе нет пункта «ЧИТЫ» и не срабатывает код разработчика.

## 2026-09-10 — Сетка дивизионов без V8 в хламе

- Исправлено: пустой пул сетки больше не пускает сток и V8 в 1 дивизион. Класс кузова сохраняется.

## 2026-09-10 — Прогон заезда: звук, мятость, дождь

- Добавлено: слои повреждений `01_damage_1`…`6` у «Грязевого дьявола»; Electron-смоук проверяет ближний/дальний выстрел, пак GTR и дым корпуса.
- Изменено: дождь quarks рождается над кадром, холст не рисует капли, пока живы quarks.

## 2026-09-09 — Лица гонщиков DiVANEngine

- Добавлено: в десктопе догрузка портрета и роста идёт через `faces.js`. Список файлов в папке `players/NN` остаётся в `chars.js`.

## 2026-09-09 — Кузов, портреты и гейт VFX DiVANEngine

- Добавлено: в десктопе кузов, портреты и гейт частиц идут через `car-paint.js` / `portraits.js` / `vfx.js`. Сами quarks остаются в `vfx/`.

## 2026-09-09 — Гараж-действие и модалки DiVANEngine

- Добавлено: в десктопе тюнинг из гаража и окна «опасно / выход» идут через `garage-act.js` / `dialogs.js`.

## 2026-09-09 — Настройки и экран карьеры DiVANEngine

- Добавлено: в десктопе клик настроек и экран «что дальше» идут через `settings-input.js` / `career-ui.js`. Призы карьеры остаются в `career.js`.

## 2026-09-09 — Клавиатура хаба и слоты DiVANEngine

- Добавлено: в десктопе меню, слоты и пауза идут через `press.js` / `slots.js`. В `rnr.html` хук `press` сохранён для браузера.

## 2026-09-09 — Трофеи, читы и клик DiVANEngine

- Добавлено: в десктопе трофеи, читы, сетка трасс и клик хаба идут через `extras.js` / `pointer.js`. В `rnr.html` хук `hubClick`.

## 2026-09-09 — Старт, интро и настройки DiVANEngine

- Добавлено: в десктопе ставка перед боем, комикс-интро и настройки идут через `prerace.js` / `intro.js` / `options.js`.

## 2026-09-09 — Гонщик и прокачка DiVANEngine

- Добавлено: в десктопе выбор гонщика, тренажёрка и оружейка идут через `roster.js` / `training.js`.

## 2026-09-09 — HUD и карусель DiVANEngine

- Добавлено: в десктопе миникарта, кадр HUD и карусель кузовов идут через `hud.js` / `carousel.js`.

## 2026-09-09 — Гараж и арена DiVANEngine

- Добавлено: в десктопе гараж, доска результатов и слои трассы идут через `hub.js` / `arena.js`.

## 2026-09-09 — Меню и камера мира DiVANEngine

- Добавлено: в десктопе титульное меню и камера заезда идут через `screens.js` / `render.js`.

## 2026-09-09 — Ввод и сцена DiVANEngine

- Добавлено: в десктопе шаг заезда и камера идут через `scene.js`, руль — через `input.js`.

## 2026-09-09 — Мир и коллизии DiVANEngine

- Добавлено: в десктопе столкновения машин и контакт мира идут через модули движка.

## 2026-09-09 — Сейвы DiVANEngine на диск

- Изменено: в десктопе карьера читает диск профиля; браузер по-прежнему localStorage.

## 2026-09-09 — Контракт DiVANEngine

- Добавлено: в `rnr.html` и `Editor.html` meta `divan-engine`. Клиент в `Full Games` вешает модули DiVANEngine по файлу и meta, не ищет `stepVehicle` в HTML.

## 2026-09-08 — Ссылка на лаунчер с GitHub

- Изменено: на сайте и в README скачивание ведёт на MSI `launcher-0.2.2.4`, не на Яндекс Диск.

## 2026-09-08 — Лаунчер обновляет сам себя

- Добавлено: установленный клиент качает свой MSI с GitHub (тег `launcher-*`) отдельно от zip игры (`game-*`).
- Добавлено: выкладка оболочки — `Full Games/Выложить-лаунчер.bat`.

## 2026-09-08 — Шрифт Bender и сигнал «Играть»

- Изменено: лаунчер набирает Bender из `assets/fonts/bender`, файлы копируются в медиа MSI.
- Добавлено: короткий PianoHit, когда обновление легло и кнопка «Играть» стала активной.

## 2026-09-07 — Релиз 0.2.2.0: лаборатория звука и HUD

- Добавлено: zip с панелью двигателя, паками моторов, HUD. Заезд читает audio из car.json.

## 2026-09-07 — Релиз 0.2.1.8: мотор в лаунчере

- Исправлено: в установленном клиенте мотор и шины идут через HTMLAudio, zip `kolesnica-content-0.2.1.8`.

## 2026-09-07 — Мотор и шины в десктопе

- Исправлено: клиент `Full Games` отдаёт WAV мотора и шин буфером по `rnr://`, иначе decodeAudioData молчит.
- Исправлено: запасная пила не глушится, пока семпл ещё не заигрался.

## 2026-09-06 — Выкладка через VPN

- Исправлено: заливка на GitHub не идёт через прокси VPN и HTTP/2.

## 2026-09-07 — Мотор и шины в zip и git

- Исправлено: WAV мотора и шин и скрипты `car-audio` / `car-tires` не были в HEAD — Pages и релиз без них. Zip после сборки проверяет, что якоря внутри архива.

## 2026-09-07 — Звук машин в стартовом меню

- Добавлено: на титуле демо-заезд играет семплы мотора, шин, нитро и дальнего ствола; в ленте выбора — холостой выбранного кузова.

## 2026-09-07 — Не грузить лишние клипы

- Изменено: мотор, шины и нитро качают только тот WAV, который сейчас нужен. Запасной пак и чужие клипы не тянутся заранее.

## 2026-09-07 — Звуки авто в продакшене

- Исправлено: WAV двигателя, дрифта, нитро, паков моторов и стволов не были в git — Pages и zip с CI уходили без них.
- Исправлено: если семпл мотора не открылся, пила не глушится. Пути с пробелами кодируются для fetch.
- Добавлено: упаковка zip падает, если нет якорных клипов мотора / шин / NOS.

## 2026-09-06 — Дальний ствол соперников

- Изменено: чужой выстрел всегда дальним клипом; громче вблизи, тише вдали; лево/право и верх/низ по положению на трассе, стереофайл не схлопывается в моно.

## 2026-09-06 — Сброс газа на финише и выстрел игрока

- Исправлено: на финише клип сброса газа (`003`) доигрывается до конца, затем холостой (`005`), если машина стоит.
- Исправлено: свой ствол каждый выстрел играет ближний клип «от себя», дальний остаётся только у чужих машин.

## 2026-09-06 — Звук ствола пишется в car.json

- Исправлено: выбор папки и клипов ствола/ульты больше не терялся при сохранении — слушатели писали в копию `audio`, а не в машину.

## 2026-09-06 — Звук ствола и пак двигателя в лаборатории

- Добавлено: у машины выбор пака двигателя из `assets/sounds/cars/engine` (прослушать набор и отдельные клипы).
- Добавлено: у ствола и ульты — набор из `assets/sounds/weapon`, клип «от лица игрока» и дистанционный, с прослушиванием.
- Добавлено: выбор пишется в `car.json` (`audio`), в заезде играет семпл вместо синтеза, если пак выбран.

## 2026-09-06 — Починка вёрстки сайта

- Исправлено: скрины заезда больше не раздуваются на всю высоту окна — кадры в сетке с потолком по высоте.

## 2026-09-06 — Скрины и роадмап на сайте

- На презентации: скрины заезда, полигона и лаборатории.
- Блок «План»: что уже в клиенте и что дальше по ROADMAP.

## 2026-09-06 — Звук двигателя

- Добавлено: у кузова папка `sound/` с пятью WAV (набор скорости, полная скорость, сброс хода, езда, стоянка). Нет папки — набор `assets/sounds/engine/`. Пока свои клипы только у «Грязевого дьявола».
- Изменено: двигатель как в GTA SA: `001` — разовый набор с рывками передач, затем луп `002`; сброс газа — `003`.
- Исправлено: на старте, финише и при нулевой скорости только холостой, не набор; обновление громкости двигателя больше не рвёт кадр.
- Исправлено: луп езды без паузы на стыке; клипы двигателя не накладываются.
- Исправлено: после трамплина мотор сбрасывает ход (`003`) и снова набирает (`001`), как скорость после удара и амортизаторов.
- Исправлено: у «Грязевого дьявола» (`01`) мотор снова слышен — свои WAV больше не глушат запас, пока клип не декодирован.

## 2026-09-06 — Реплики NPC сетки

- Добавлено: у заполнителей `07`–`12` полный банк `voice/lines.json` (те же события, что у сюжетных пилотов), 21+, свой характер, три дубля, пустые MP3 и лист `СЧИТАТЬ.md`.

## 2026-09-06 — Повреждения корпуса

- Добавлено: у «Грязевого дьявола» шесть слоёв кузовных повреждений (`assets/data/cars/01/damage`), чем меньше HP — тем сильнее мятый корпус.
- Добавлено: на исходе с корпуса идёт чёрный дым и огонь (three.quarks); без слоя частиц — запас на холсте.

## 2026-09-06 — Новая ссылка на лаунчер

- Скачать клиент: https://disk.yandex.ru/d/BHvSSYPNRSt8cQ

## 2026-09-06 — Версия установщика

- Исправлено: electron-builder падал на `0.2.1.3`. Для npm — `0.2.1-3`, номер игры по-прежнему четыре части.

## 2026-09-06 — Быстрее выкладка zip

- Изменено: `Выложить-игру.bat` не ставит npm заново и пакует контент без второй копии и без бесполезного сжатия.

## 2026-09-06 — Средний класс

- Добавлено: пять магазинных кузовов между хламом и V8 — «Такси», «Пикап», «Купе», «Фургон», «Скорая» (слоты 17–21, 1400–3000$).
- Добавлено: свои киты (счётчик, масло, фара, дверь, шприц) и ульты (гудок, откид, обгон, шторка, сирена).
- Изменено: в дивизионах 3–4 ИИ едут на хламе и среднем классе, V8 и выше — с дивизиона 5.

## 2026-09-06 — Сайт: только клиент, рост влезает, кадры из пролога

- С презентации убраны ссылки «играть в браузере».
- Досье гонщика занимает один экран: фигура целиком, текст поверх правого края.
- Блок кадров берёт семь сцен и тексты из `assets/data/cats/00`.

## 2026-09-06 — Досье гонщика влезает в окно

- Ростовой кадр больше не обрезает ноги: высота досье привязана к экрану.
- Комикс-кадры идут лентой с горизонтальной прокруткой, а не сеткой в семь колонок.

## 2026-09-06 — Починка презентации Pages

- Фон, пролог и шрифт больше не 404: в git лежат JPEG героя, комикс-кадры и Bender.
- Починены обрез заголовка, битая лента и пустая дыра секции мира.

## 2026-09-06 — Презентация на GitHub Pages

- Корень сайта — лендинг «Колесница войны»: пролог, гонщики, комиксы, кадры заезда.
- Скачать клиент ведёт на Яндекс Диск. Браузерная игра осталась по адресу `rnr.html`.

## 2026-09-06 — Музыка в репозитории

- В git и на Pages лежит `assets/music/` (меню, гараж, интро, гонка, Load). Сайт больше не зависит только от CDN.

## 2026-09-06 — Кадры пролога в клиенте

- Исправлено: картинки катсцены в установщике грузятся через fetch/blob, как остальной контент. `img.src` по `rnr://` оставлял чёрную панель.

## 2026-09-06 — Заставка в установщике без сети

- Исправлено: офлайн-клиент больше не ждёт Google Fonts (чёрное окно вместо «нажмите любую кнопку»). Шрифты с диска. Пролог не роняет кадр, если картинка или VHS сбоит.

## 2026-09-06 — Папки машин

- Изменено: кузов и броня каждой машины лежат в `assets/data/cars/NN/` вместе с `car.json`. Слои брони — в `armor/`. Старая куча в `assets/machines/cars/` опустошена.

## 2026-09-06 — Пролог снова после заставки

- Семь кадров снова сразу после «нажмите любую кнопку», затем меню. Новая игра и камера идут без повторного пролога.

## 2026-09-06 — Пролог после камеры

- Семь кадров сеттинга идут после «Новая игра» и настройки камеры, не после заставки «любая кнопка». Дальше — выбор гонщика.

## 2026-09-06 — Пролог с интригой

- Текст заставки тянет в сеттинг: слухи, пропуск без имени, «зачем тебя пустили». Сюжет не раскрывается.

## 2026-09-06 — Пролог: сеттинг, не сюжет

- Семь кадров интро говорят про Чёрный Пояс, арену и смертельный заезд. Сюжет кампании в заставке не раскрывается.

## 2026-09-06 — Пролог по СЮЖЕТ.txt

- Текст семи кадров пролога сжат из `assets/data/players/СЮЖЕТ.txt`: мир, арена, «Ночь зубов», Лаурят, семья, вход Медведя.

## 2026-09-06 — Пролог Чёрный Пояс

- Добавлено: после «нажмите любую кнопку» и загрузки — 7 кадров сеттинга. Текст и картинки: `assets/data/cats/00`. Музыка: `assets/music/Load`. ENTER дальше, пробел удержать — пропуск.

## 2026-09-06 — Сдача после покупки хлама

- Исправлено: покупка Тазика и другого хлама сразу даёт два уровня сдачи (двигатель и шины) и открывает гараж — без чёрного кадра.

## 2026-09-06 — Выкладка с номером вручную

- Добавлено: в `Выложить-игру.bat` сам пишешь версию — обновить игроков или только заменить файлы на том же номере.

## 2026-09-06 — Версия 0.2.1.0

- Изменено: десктоп-клиент и пакет контента — `0.2.1.0`, чтобы лаунчер предложил обновление после перезаливки файлов.

## 2026-09-05 — Сюжет: Скарабей

- Изменено: канон `DOCK.md` и `СЮЖЕТ.txt` — Медведь умирает, спасая Янот; три старых финала больше не равны канону.
- Добавлено: вторая кампания — выбор Ерш / Янот / Бегемотик / Башкир; Скарабей как временный босс; играбелен после сброса ошейника, только если изначально играли Медведем.

## 2026-09-05 — Выбор гонщика

- Исправлено: с экрана выбора игрока можно вернуться в главное меню — ESC или кнопка «Назад в меню».

## 2026-09-05 — Аватары НПС сетки

- Добавлено: портреты заполнителей заезда — Зайфер, Дизель, Джо, Пёс, Шрам, Мамаша (`07`–`12`). Круглый кадр как у сюжетных пилотов. В HUD и на ставке больше нет серых заглушек.
- Добавлено: ростовые кадры тех же шестерых — `NN_Player_fullbody.webp`, фигура без фона (прозрачный WebP). Витрина гаража и тренажёрки больше не падает на портрет.
- Изменено: случайные ИИ берутся из `CHARS` с флагом `filler`, не из отдельного списка без картинок.

## 2026-09-05 — Стартовый хлам

- Добавлено: пять магазинных кузовов слабее всех — «Тазик», «Каблук», «Жезл», «Козёл», «Борт» (слоты 12–16, 200–400$, этап 0). Касса старта 1000$: после Борта остаётся 600$ на шины, после Тазика — 800$ на амортизаторы или нитро.
- Добавлено: свои киты (канистра, болты, жезл, подстволье, лом) и ульты (хлопок, сброс кузова, мигалка, лебёдка, отцеп).
- Изменено: в дивизионах 1–2 ИИ и босс едут только на этом хламе, не на V8 / Шредере / Фантоме.
- Добавлено: после покупки — сдача кассы: два уровня на пустые слоты тюнинга (без брони).

## 2026-09-05

- Десктоп: лаунчер сразу с фоном, музыкой Load и кнопкой звука; лаборатория только из `Лаборатория.bat`.
- Десктоп: убрана плашка «пока нет файла» — кадр уже лежит в лаунчере.
- MSI лаунчера: русский неформальный мастер, папка `C:\games\Divan Games\Колесница войны`, ярлык «Колесница Войны».
- Десктоп: подпись установщика издателем Divan Games.
- Лаунчер после скачивания игры не зависает на «размер не совпал» у car.json.
- Десктоп: коллизия объектов в заезде снова работает — движок клиента вызывает `pushCar`.
- Лаборатория: свободный режим — клик по объекту сразу двигает гизмо; старт/финиш — отдельная клетка (двигать, крутить, удалить, снова добавить).
- Лаборатория: браузер контента в пустом месте слева под холстом, рядом с карточкой трассы.
- Лаборатория: вместо деколей — полка ассетов (паки, карточки, плюс и дроп). Двойной клик открывает редактор слоя и коллизии.
- Погода: больше частиц дождя, снега и песчаной бури на биомах (извержение без изменений).
- Лаборатория, карта: гизмо объекта (оси, кольцо поворота, галочка), кисть деколей, трамплины с заходом.
- Лаборатория, вкладка «Карта»: редактор трассы (контрольные точки, биом, покрытие, деколи, трамплины / мины / масло / пады, срезы). Ctrl+S пишет JSON в `assets/data/tracks/`; опубликованная петля появляется в «Выбор трассы». «Тест трассы» открывает заезд и по ESC возвращает на карту.
- Оружейка в гараже: качается ствол и ульта выбранного кузова (6 уровней). Урон, КД, магазин, перегрев, длительность и радиус ульт растут с уровнем. Старые сейвы дописывают `wep`/`ult`.
- У каждого стокового кузова своё оружие и ульта: больше нет клона «двустволка + бычий рывок» на Дьяволе, Урале, Курьере и Шпильке. Наборы в `combat-kits.js`.
- «Прицел», гаубица, плазма, пилы, шипы, мина-посылка, заносные гвозди, скоба и купол (только пули/мины) работают по правилам, а не как одна пуля вперёд.
- «Жеребец» без смены фантазии: миниган, прыжок, «Табун».

## 2026-09-04

- Погода: дождь, снег и песчаная буря рисуются three.quarks по камере заезда; капли на стекле убраны. Молния и дымка остаются на холсте. При «Частицы: низкое» или сбое WebGL — прежние осадки на canvas.
- Погода: дождь, снег и буря читаются (полоски / хлопья / зерно по ветру), не стена и не пустота. На «Вулкане гибели» — извержение: тёмный пепел и огненные искры.
- Лаунчер: нижний статус компактный, слева; в разработке «Играть» не блокируется из‑за размера файлов.
- Интро: удержание пропуска — компактное кольцо в левом нижнем углу, больше не перекрывает текст главы.
- Погода трассы больше не случайная: дождь на «Перекрёстке смерти» (молния, капли на стекле), снег на «Ледяном перевале», песчаная буря на пыльном овале и в каньоне. С ручника на снегу летит снег, по лужам — брызги воды.

- Музыка читается из `assets/music/` (те же папки и имена: `main/0.mp3`, `change/1.mp3`…). Если файла нет — запасной URL на `ikrinka24.com`.
- Плейлист больше не прописан вручную: игра берёт все треки из папки и крутит случайно (один файл — он один).
- Эффекты читаются из `assets/sounds/FX/` (`money.mp3`, `CashBay.mp3`, `carPay.wav`); если файла нет — тот же путь на хосте.
- Десктоп-клиент вынесен в `Full Games` (лаунчер, проверка файлов, окно вне браузера). Код этой браузерной игры не менялся.
- Лаунчер клиента: арт на весь кадр, стекло слева, пепел, окно 1280×720 без растягивания.
- Главное меню: пункты компактнее, «Выход» не обрезается.

## 2026-09-01

- Старт: загрузка больше не обрывается через 8 секунд. Меню ждёт гонщиков, комиксы, машины, карту, SFX и все треки из `music.js`; прогресс пишет этап (Комиксы / Машины / Музыка). Подсказка про VPN — с 12-й секунды, без принудительного входа.
- На титуле одноразовое донесение: игра выйдет из браузера, клиент сам подтянет дополнения; ради стабильности и онлайна (рекорды, друзья). Закрывается на «Понятно» / Enter / Esc.
- У Янот комикс-интро: 7 кадров из `assets/data/players/06/comics/`, музыка `intro/06.mp3`, титры из сюжета.

## 2026-08-31

- После финиша карьера больше не прыгает сразу в гараж: подиум показывает приз 1–3, затем экран «что дальше» — серия побед, бонус за три этапа, почему открылся кузов, реванш. Пять побед открывают выбор уже проеханной трассы. Реванш не двигает номер этапа.

## 2026-08-30

- Управление: газ больше не залипает после меню и Alt+Tab; Enter / NumpadEnter / пробел / Esc одинаково подтверждают и закрывают экраны; Z/X/C снова стреляют, если в сейве остались старые Ctrl/Alt. Демо-заезд на фоне меню больше не подменяет машину игрока.

## 2026-08-29

- «Шредер»: пилы снова под кузовом, зубья выглядывают из крыльев.
- Эксперимент VFX: взрывы, пыль, дуло, нитро и шлейф ракеты через three.quarks (ортослой, блют в canvas до HUD). При «Частицы: низкое», reduced-motion или сбое WebGL — прежние круги. Откат `restore-quarks-rollback.bat`.
- Нитро: струя с труб как была; рядом не искры, а полоски ветра. Приземление с высоты: пыль плюс искры днища об асфальт.
- Искры посадки: в 10 раз мельче, в 5 раз гуще, спавн строго по контуру альфы кузова, разлёт наружу по нормали.
- Искры посадки увеличены в 5 раз — предыдущий размер на трассе не читался.
- Подставка гонщика: прежний тёмный овал с цветной кромкой, выше таблички с именем.
- Ветер нитро — вертикальные ленты позади кузова. Искры посадки — скрежет корпуса. Взрывы разделены: машина / мина / ракета.
- Музыка с `ikrinka24.com`: к списку добавлен `MUSIC_CDN_VER`, чтобы браузер не держал старый `main/0.mp3` после замены на хосте.
- Выбор гонщика: случайный дубль `name` из `lines.json` и выноска с текстом. Старый голос с ikrinka / `VOICE_TRACKS` отключён.
- Старт: битые настройки звука больше не останавливают загрузку; HTTP 404 не ждёт картинку по 12 секунд. `start.bat` проверяет живой HTTP на 8765 и перезапускает зависший Python, а не открывает мёртвый порт.
- После финиша машины катятся дальше, тормозят к обочине и объезжают других. Столкновение на финише только сдвигает, без урона и взрыва.
- Банк реплик: у каждого гонщика и у Бориса Быка папка `assets/data/players/NN/voice/` (`lines.json` + три пустых MP3 на событие). Ведущий арены — `host/voice/`. Лист записи — `VOICE.md` и `СЧИТАТЬ.md`.
- Выноска с текстом реплики у аватара в колонке мест на HUD (старт, лидер, погоня, таран, удар, нитро, килл, круг, финиш).
- Реплики Бегемотика, Башкира, Быка, Янот и ведущего дописаны в том же тоне, что у Медведя и Ерша: длиннее, с панчлайном.
- Полигон лаборатории: два трамплина на противоположных прямых, без мин и ящиков.
- Выбор гонщика на широком экране: лента и пепел идут по всему холсту (поля ultrawide), а не обрываются на 16:9. Если холст меньше окна — те же диагонали на фоне страницы.
- Старт: сначала «нажмите любую кнопку», затем загрузка файлов под музыку главного меню, после неё сразу меню. На обоих экранах — логотип, живые диагонали как у достижений и пепел.
- Баланс карьеры: цены на кузова и запчасти выше, призы ниже, машины открываются позже. Боссы в первом дивизионе на стоке, тюнинг и резинка ИИ растут медленнее.
- Ставки: экран как хаб (рост выбранного, кэфы 1/2/3, сумма с кассы). В сетке только игрок, босс Бык и три ИИ — без остальных сюжетных пилотов.

- Гонщики переехали в `assets/data/players/NN/` (портрет, рост, `comics/`, сюжет). Добавлен Янот — хозяйка «Брички».
- Стартовые пипсы «Брички»: макс 1, разгон 2, руль 2, корпус 1.
- Рост из папки гонщика: `NN_Player_fullbody`, `NN_fullbody` или `_fullbody` (webp/png), без кэша 404.
- Лаборатория: личность авто — все гонщики из игры (включая ИИ); новые аватары `NN_Player` сами появляются в сетке.
- Лаборатория: отмена и повтор шагают только по выбранному авто, до конца его истории, не по чужим слотам.
- Фон: клетка в лаборатории, на полигоне и повтор тайлов трассы вчетверо мельче.
- Лаборатория: слой «Броня» открывает сетку База–VI; клик сразу показывает тюнинг корпуса на холсте.
- Приземление: кузов садится и покачивается, колёса выглядывают из арок не дальше 5% своей посадки, скорость падает (амортизаторы режут штраф). Без перекоса спрайта.
- Карусель машин: сначала магазин по возрастанию цены, затем личные кузова гонщиков. Новая машина без владельца сама встаёт в магазин по цене; слоты ассетов `01`…`NN` не сдвигаются.
- Физика машин: инерция при отпускании газа, ручник на пробел, лёгкий занос в поворотах (шины и управление его душат). Приземление с любой высоты — пыль, колёса чуть выходят из арок, потеря скорости зависит от амортизаторов.
- Лаборатория: «Личность авто» — магазин или хозяин (Медведь / Ерш / Бегемотик / Башкир); в игре кузов встаёт в карусель как личный или в магазин.
- Лаборатория: Ctrl+S сохраняет настройки; окно показывает успех или сбой сохранения, загрузки, бэкапа, импорта и экспорта.
- Лаборатория: копирование ширины и высоты между слоями одного типа (колесо → колесо, нитро → нитро).
- Лаборатория: «Сохранить настройки» пишет базу слота (как раньше «Сохранить как базовые»); клик по пустому холсту снимает выбор слоёв.
- Лаборатория: Shift во время перетаскивания сразу режет жест по горизонтали или вертикали, даже если клавишу нажали после старта.
- Лаборатория: после обновления кода остаётся сохранённый слот, а не заводская база; галочки сетки / зеркала / брони помнятся.
- Лаборатория: несколько слоёв сразу — Ctrl+клик добавляет, Shift+клик берёт диапазон (панель и холст); клик по уже выбранному двигает всю группу.
- Лаборатория: выбор нитро больше не подсвечивает и не двигает колёса; клик по пустому месту не сбрасывает слой.
- Лаборатория: клик по пустому месту на холсте (в том числе по рамке кузова) больше не сбрасывает выбранный слой колеса или нитро.
- Полигон: заезд стартует после загрузки, а не в середине скрипта — заставка больше не зависает на 0%.
- Полигон лаборатории: отдельная простая трасса «Полигон» (не в карьере). Фон — клетка как на холсте, дорога-прототип, линия старт/финиш, таймер текущего / прошлого / лучшего круга. Круги бесконечные, ESC — назад.
- Лаборатория: пара колёс едет зеркально (общий X, Y в разные стороны), даже если оба слоя в выборе. Шаг назад дописывает жест и откатывает его; шаг вперёд — Ctrl+Y / Ctrl+Shift+Z, в том числе на русской раскладке.
- Лаборатория: поля кузова / колёс / нитро только у выбранного слоя; отмена и повтор идут по всей истории; сброс ↺ у каждого слоя к базе.
- Лаборатория: выбор на холсте совпадает со слоями и полями (как в Photoshop); зеркало оси подсвечивает пару колёс и правит именно её.
- Лаборатория: кузов по умолчанию тащит броню; галочка снимается — корпус едет один. Слой «Броня» двигается отдельно (смещение `body.ax` / `body.ay`).
- Лаборатория: слои сразу под слотами, клик по колесу/нитро на холсте двигает деталь (не панораму), зеркало оси лево↔право, сетка на всё поле шагом 2.5 и включена сразу, отметки тонкие.

## 2026-08-28

- Лаборатория: каждый клон колеса и нитро — отдельный слой (как в Figma); нитро можно держать под кузовом или над ним. Канон слота лежит в `assets/data/cars/NN/car.json`; игра и редактор читают эти файлы, свежие правки в браузере перекрывают диск по `rev`. Сохранение на диск — POST `/__save-car` через `tools/rnr-http.py` (запуск `editor.bat` / `start.bat`).
- Экран загрузки больше не зависает на 0%: пакет лаборатории читается после объявления кэша кузовов; 404 не держат заставку; через 8 с игра открывается в любом случае.
- Настройки разбиты на «Настройка графики», «Настройки звука» и «Настройка игры». Раскладка клавиш — пункт «Настройки управления» внутри игры. В графике ползунок камеры 1.75–2.2: на фоне крутится демо-заезд, зум меняется сразу. По умолчанию камера чуть дальше (2.0 вместо 2.25).
- В режиме разработчика в меню есть «Лаборатория». Перед входом — предупреждение: можно испортить машину; если не из команды — лучше не заходить. Нет / Да.
- Достижения: крупные медальоны на плитках 3×3, плотный медленный фон-штриховка, назад по Esc.
- «Сброс прогресса» обнуляет только текущую карьеру. Слоты сохранений и настройки остаются; слот можно стереть в экране загрузок клавишей Delete.
- «Бричка» (`11.webp`): класс 1, $800 со старта. Слабые Макс / Разгон / Корпус, руль +1. Пуля прямо, нитро, ульта «Купол» 10 сек от всего урона, пассив пылесосит деньги, ремонт и ускорение рядом.
- «Урал»: колёса в арках (слой под кузовом), раскладка 6×6 по спрайту; ширина/высота в лаборатории = размер на холсте и в гонке без лишнего множителя. Старые сохранения с дисками поверх кабины переезжают сами.
- Игра читает пакет лаборатории тем же `editor/data.js`, что и редактор: колёса, масштаб, слои и броня совпадают с холстом. Гаражный тюнинг больше не подменяет броню, если слот настроен в лаборатории.
- Лаборатория: ширина и высота колеса пишут в модель; в центре диска точка оси; колёса по стеку слоёв.
- Наведение на колесо или нитро сразу показывает его цифры в панели; клик тоже подставляет текущий слот.
- Лаборатория: панель сначала кузов / слои / колёса, характеристики ниже; клик по холсту больше не ломает жест; правки сами пишутся в браузер, игра видит те же колёса без отдельного «Сохранить».

## 2026-08-27

- На каждой трассе участки покрытия (асфальт, песок, грунт, трава, лёд, снег, лава) запекаются в пререндер дороги.
- Фон трассы: исходник один раз жмётся в 512 px, в кадре — лёгкий повтор (без фильтрации 1000 px каждый кадр).
- «Перекрёсток смерти»: фон из тайлов `assets/image/textures/map/garden/` (`.webp` или `.png`).
- Щит в гонке: полупрозрачная заливка и контур по маске кузова, бегущая вспышка; кружок больше не рисуется.
- Тень машины: силуэт по альфе кузова без брони (не прямоугольник); в полёте меньше и бледнее.
- Чит `deliane` сразу открывает выбор гонщика. В режиме разработчика можно сесть на любой кузов, в том числе личный чужой. На главной, в паузе гонки и в паузе гаража — пункт «Выбор трассы» с плитками и быстрым стартом.
- Столкновения машин, пуль, мин и шипов: ориентированный прямоугольник кузова (как рамка в лаборатории), у каждого слота свой размер. SVG-коллизии нет.

## 2026-08-26

- «Жеребец» (`10.webp`): дорогой кузов, быстрый с средней бронёй. Миниган на 50 патронов (шкала = магазин, перегрев после опустошения), прыжок вместо нитро, ульта «Табун» сбрасывает скорость и отталкивает рядом.
- Клавиши гонки: оружие Z / P, нитро X / {, ульта C / }. Ctrl, Alt и пробел больше не стреляют.
- HUD в гонке: меньше панелей, воздух, живые анимации (место, круг, HP, скорость, лидеры, скилы). Деньги с трассы убраны.
- Камера гонки ближе (зум 2.25): машина крупнее в кадре, HUD без изменений.
- «Пыльный овал»: фон из тайлов `assets/image/textures/map/sand/` (каньон остаётся на `desert/`).
- «Ледяной перевал»: фон из тайлов `assets/image/textures/map/snow/` (те же правила нумерации, что у пустыни).
- VHS на миниатюре машины в гараже и на карточках выбора автомобиля (только превью, не весь UI).
- VHS на комикс-интро: тот же эффект, только на верхней панели с кадром (текст главы без наложения).
- VHS на главном меню: строки развёртки, зерно, бегущая дорожка и редкий сдвиг кадра (при «меньше движения» — только строки и виньетка).
- Демо на меню: зум как в гонке, машины таранят и стреляют, на трассе мины / масло / трамплины; камера переключается на убийцу.
- Первый экран — загрузка с реальным прогрессбаром; игра не стартует, пока не подтянуты картинки, шрифты и трек меню. Если дольше 8 секунд — подсказка отключить VPN.

## 2026-08-25

- Тренажёрка и гараж: слева витрина пилота в рост (`NN_Player_fullbody.webp`), прожектор и табличка с именем.
- Экран результатов: карточки по местам с крупным портретом без подложки, машиной и статами; лидеры выезжают по очереди, реплика диктора — в карточке гонщика.
- Перед меню — стартовый экран с логотипом и мигающей надписью «Нажмите любую кнопку» (клавиша, клик или геймпад).
- В списке лидеров на трассе и на экране результатов — аватарки; нет файла — пустой слот.
- Борис Бык (аватар `05`) пока только соперник на трассе: занимает один слот вместо случайного ИИ.
- Слои: порядок и галочки сохраняются (в том числе у «Урала» — загрузка больше не откатывает стек к заводу); игра рисует тот же порядок, что лаборатория.
- «Урал»: жёсткий стек слоёв — кузов, броня, колёса в колодцах, нитро сверху; без полупрозрачного дубля колёс.
- Слои: блок сразу под кузовом; список сверху вниз (верхний поверх); галочка прячет слой; колёса под кузовом видны полупрозрачно, иначе их не заметить.
- После лаборатории передние колёса снова поворачивают: игра и загрузка редактора восстанавливают флаг руля, если он потерялся в сохранении.
- В лаборатории зум холста: кнопки − / % / +, колесо мыши, клавиши + − 0; «В кадр» учитывает кузов, колёса и нитро, крупные машины больше не упираются в предел масштаба.
- На вкладке игры золотая корона: ico/png фавикон на игре, входе и в лаборатории.
- В лаборатории снова работают слои: список заполняется, слой выбирается кликом, галочка сразу прячет его, выбранный слой двигается на холсте.
- С колёс в игре сняты зелёные контрольные точки: посадка настраивается в лаборатории.
- В лаборатории можно выбрать несколько слоёв или колёс (Ctrl+клик) и двигать их мышью или стрелками.
- В лаборатории: «Отмена» / «Вперёд» (Ctrl+Z / Ctrl+Y) — шаг назад и шаг вперёд.
- «Урал» Башкира: слои как в лаборатории (кузов → колёса → броня), без подмены кузова одним спрайтом.
- Лаборатория машин: живой холст (масштаб, панорама, перетаскивание), характеристики, свои слоты, колёса с рулём и зеркалом.
- Игра читает из редактора статы, ховер, цвета, имена и дополнительные машины; в гараже клавиша E открывает лабораторию.

- Колёса снова под кузовом (пилы Шредера между колёсами и корпусом), а не поверх машины.
- В каждой гонке три невыбранных гонщика выходят боссами на фирменных машинах (Дьявол / V8 / Шредер / Молот); ещё двое — случайный ИИ на свободных кузовах.
- «Шпилька»: нитро придвинуто к корпусу и рисуется под кузовом.
- На выборе гонщика при активной карточке играется голос имени (`01_Player`–`04_Player`).

## 2026-08-24

- Аватар владельца на карточке машины крупнее и без цветного кольца.
- Аватар хозяина машины — крупный портрет в правом нижнем углу карточки, не на превью трассы.
- «Урал»: колёса снова в колодцах превью; кузов на карточке уменьшен, чтобы влезал в рамку.
- «Курьер»: нитро ближе к корпусу и под слоем кузова.
- «Шпилька» (`09`): колёса по зелёной разметке, нитро — четыре трубы сзади.
- В гонке и результатах вместо «ВЫ» пишется имя выбранного гонщика.
- Экран сравнения в автопарке: живые превью, VS, шкалы дуэлью и крупный аватар хозяина.
- Визуальная броня: слои `NN_armor_1.webp` … `NN_armor_6.webp` подставляются по уровню тюнинга брони.
- Кадр 3 интро Башкира обновлён.
- Камера гонки ближе (зум 1.55): машины и трасса крупнее, HUD без изменений.
- Минный иммунитет Башкира: «УКЛОН!» один раз на мину (вход в радиус), плюс антиспам тоста — без ленты текста по трассе.

## 2026-08-23

- В гараже машина в превью едет, как на выборе, и крупнее.
- Нитро под кузовом: у Дьявола 4 малых трубы, у V8 / Шредера / Молота — по 2, у Фантома — 2 широких сопла.
- Пламя ускорения рисуется под кузовом, а не поверх корпуса.
- Выбор машины: ровные карточки как в гараже, широкая трасса в превью (полоса как на карте), разметка едет быстрее.
- У Башкира своё комикс-интро (7 кадров, музыка `intro/04.mp3`).
- У Бегемотика своё комикс-интро (6 кадров, музыка `intro/03.mp3`).
- Интро Медведя: кадр целиком (без вырезания), тёмный фон по краям, зацикленная музыка, финал — `7.png`.
- В гоночный плейлист `music.js` добавлены треки `racing/9.mp3`–`racing/13.mp3` с тем же хостом.

## 2026-08-22

- Тосты комментатора: перенос строк, клип по панели, ширина по тексту — реплики больше не вылезают за рамку.
- HUD гонки: кокпит снизу (спидометр, корпус, скилы), место над стрелкой игрока, миникарта с рельефом и обрезкой по рамке.
- Экраны результатов и достижений переоформлены: косые панели, золотая строка игрока, карточки статистики и сетка трофеев.
- Перед гонкой показываются рекорды выбранной трассы; на миникарте — мины, бусты, трамплины и ящики.
- Грязевой дьявол: колёса ещё на 5% ближе. Неон Аэро-клинка приглушён.
- На главной добавлен пункт «ЧИТЫ»: код `deliane` включает режим разработчика (деньги, все машины, все этапы разблокированы).
- У `03.webp` / Шредера добавлены постоянно вращающиеся циркулярные пилы между кузовом и колёсами.
- Добавлены WebP-кузова `01.webp`–`06.webp` в стиле референса; загрузка машин теперь предпочитает `.webp`.
- Тренажёрка и автопарк в том же стиле, что гараж: шапка, карточки, зазоры. Автопарк — сетка 3×2, клик открывает сравнение.
- Гараж: отступы от краёв, карточки пилота и машины, список тюнинга с зазорами; тренажёрка и автопарк в ряд, гонка — отдельная кнопка.
- Выбор гонщика: ENTER / «ВЫБРАТЬ» открывают досье; из досье ENTER берёт персонажа, ESC закрывает карточку.
- Цены тюнинга в гараже снова прежние (сотни–тысячи $). Прокачка в тренажёрке остаётся дорогой.

## 2026-08-21

- Тренажёрка качает **текущего** гонщика: скорость / поворот / броня и скил (кнопки «+», стрелки вверх/вниз).
- У каждого гонщика личный скил (ур. 1–6): лечение, нитро, урон, иммунитет к минам.
- Кузова машин грузятся по цепочке `01.webp` → `.png` → `.svg` (если файла нет — следующее расширение).
- Из досье можно сразу выбрать гонщика: кнопка / Enter — к машине, Esc или клик мимо — закрыть.
- Положить кузова `01`–`06` в `assets/machines/cars/` (`.svg` / `.png` / `.webp`; нос вправо; если нос вверх — `CAR_SVG_ROT=Math.PI/2`).
- Описания гонщиков жёстче; на карточке выбора текст переносится и не вылезает за рамку.
- Гонщики переименованы: МЕДВЕДЬ, ЕРШ, БЕГЕМОТИК, БАШКИР; портреты — `assets/image/avatars/01_Player.png` … `04_Player.png`.
- Аватары без круговой обрезки и в два раза крупнее на выборе гонщика и в гараже.
- Трамплины ставятся только на длинных прямых вдали от перекрёстков; на подъезде и на рампе — стрелки направления прыжка.
- Плеер не отключал музыку из‑за `window.settings` (`let` не попадает в `window`).
- Плеер выбирает трек случайно; при смене категории сбрасывает текущий; в консоли виден выбор.
- Список треков вынесен в `music.js`; плеер больше не сканирует папки.
- Деплой GitHub Pages запускается автоматически после пуша в `main`.

## 2026-08-20

- Добавлен вход `index.html` для GitHub Pages.
- Репозиторий опубликован на GitHub; сайт отдаётся из корня ветки `main`.
## [2026-08-22] — Брутальные пиксельарт-достижения
- Добавлено: набор из 9 прозрачных постапокалиптических гоночных иконок.
- Изменено: экран достижений теперь рисует единые пиксельарт-иконки вместо эмодзи.
- Изменено: описания достижений переписаны в жёстком тоне пустоши.
## 2026-08-25 — Визуальный редактор автомобилей
- Добавлено: отдельная страница `Editor.html` для настройки кузовов, колёс и слоёв.
- Добавлено: сохранение настроек в браузере и импорт/экспорт JSON.
- Изменено: игра подхватывает из редактора точки колёс, масштаб, смещение кузова, пользовательский корпус и тюнинг брони.
