# Звук кузова

Положи клипы в `sound/` рядом с `car.json`. Имена как у общего набора: `sound_001.wav` … `sound_005.wav`.

Список и смысл файлов — `assets/sounds/engine/README.md`. Нет папки — играют стандартные.
