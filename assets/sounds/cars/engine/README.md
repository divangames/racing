# Двигатели машин

Паки для лаборатории. В слоте машины выбираете набор — в `car.json` пишется `audio.engine`.

Каждый пак: папка `NN Имя/sound/` с пятью клипами, как у `assets/sounds/engine/README.md`.
